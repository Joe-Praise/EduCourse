export type bannerTextType = {
	title: string;
	body: string;
};

export type courseCardType = {
	img: string;
	instructor: string;
	coureTitle: string;
	createdAt: Date;
	noOfStudents: number;
	price: number;
};
